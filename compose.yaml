# This docker-compose file sets up two services: kobuki and lidar_node.
# The kobuki service runs the Kobuki robot base, while the lidar_node service runs
# the SLAMTEC LIDAR node. Both services are connected to a custom Docker network
# named ros_network to facilitate communication between them.
services:
  kobuki:
    image: crl/kobuki-bringup:humble
    build:
      context: .
      dockerfile: bringup.Dockerfile
    restart: unless-stopped
    networks:
      - ros_network
    environment:
      - RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
      - CYCLONEDDS_URI=file:///cyclonedds.xml
    volumes:
      - ./cyclonedds.xml:/cyclonedds.xml
      - /dev:/dev # Testing if this solves the issue wit kobuki base not being recognized.
    privileged: true
    # devices:
    #   - /dev/kobuki:/dev/kobuki

  lidar_node:
    build:
      context: .
      dockerfile: slam.Dockerfile
    networks:
      - ros_network
    ports:
      - "8765:8765"
    environment:
      - RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
      - CYCLONEDDS_URI=file:///cyclonedds.xml
    volumes:
      - ./cyclonedds.xml:/cyclonedds.xml
      - ./maps:/ros2_ws/maps
    privileged: true
    devices:
      - /dev/rplidar:/dev/rplidar
      - /dev/input/js0:/dev/input/js0
      - /dev/kobuki:/dev/kobuki

networks:
  ros_network:
    driver: bridge
